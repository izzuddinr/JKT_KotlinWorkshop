package com.training.exercises

// To test your solutions
fun main() {
}

/**
 * Create a Function to Calculate Discount
 *
 * Write a function calculateDiscount that takes two parameters:
 *
 * originalPrice (Double): The original price of an item.
 * discountPercentage (Double): The discount percentage to be applied.
 * The function should return the final price after applying the discount. The discount should be
 * calculated as follows: finalPrice = originalPrice - (originalPrice * discountPercentage / 100)
 *
 * Example:
 * val finalPrice = calculateDiscount(100.0, 20.0)
 * println("Final Price: $finalPrice") // Should print: Final Price: 80.0
 */
fun calculateDiscount(originalPrice: Double, discountPercentage: Double): Double {
    TODO("Not yet implemented")
}

/**
 * Create a Function with Varargs for Concatenating Strings
 *
 * Write a function concatenateStrings that takes a variable number of string
 * arguments (vararg strings: String) and returns a single string with all the strings concatenated
 * together, separated by spaces.
 *
 * Example:
 * val concatenated = concatenateStrings("Hello", "world", "from", "Kotlin")
 * println(concatenated) // Should print: Hello world from Kotlin
 */
fun concatenateStrings(vararg strings: String): String {
    TODO("Not yet implemented")
}

/**
 *  Implement a Function to Count Vowels
 * Write a function countVowels that takes a string and returns the number of
 * vowels (a, e, i, o, u) in the string. The function should be case-insensitive.
 *
 * Example:
 * val vowelCount = countVowels("Hello World")
 * println(vowelCount) // Should print: 3
 */
fun countVowels(input: String) : Int {
    TODO("Not yet implemented")
}

/**
 * Implement a Function to Find the Median
 * Write a function findMedian that takes a list of integers and returns the median value.
 * The median is the middle value when the list is sorted. If the list has an even
 * number of elements, return the average of the two middle values.
 *
 * Example:
 * val median = findMedian(listOf(3, 1, 4, 1, 5, 9, 2))
 * println(median) // Should print: 4
 */
fun findMedian(numbers: List<Int>): Double{
    TODO("Not yet implemented")
}

