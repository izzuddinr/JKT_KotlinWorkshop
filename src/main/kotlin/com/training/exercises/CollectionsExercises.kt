package com.training.exercises

// To test your solutions
fun main() {

}

/**
 * Write a function removeOddNumbers(numbers: List<Int>): List<Int> that removes all odd numbers
 * from the list and returns a new list containing only the even numbers.
 *
 * Example:
 * val numbers = listOf(1, 2, 3, 4, 5, 6)
 * println("Even numbers: ${removeOddNumbers(numbers)}")  // Output: [2, 4, 6]
 */
fun removeOddNumbers(numbers: List<Int>): List<Int> {
    TODO("Not yet implemented")
}

/**
 * Write a function findLongestWord(words: List<String>): String that finds and returns the
 * longest word in the list. If there are multiple words with the same maximum length,
 * return the first one that appears.
 *
 * Example:
 * val words = listOf("cat", "hippopotamus", "dog", "elephant")
 * println("Longest word: ${findLongestWord(words)}")  // Output: hippopotamus
 */
fun findLongestWord(words: List<String>): String {
    TODO("Not yet implemented")
}

/**
 * Write a function findCommonElements(list1: List<Int>, list2: List<Int>): List<Int> that finds
 * common elements between two lists. The function should return a list of common elements, with
 * no duplicates and in the order they first appear in list1.
 *
 * Example:
 * val list1 = listOf(1, 2, 3, 4, 5)
 * val list2 = listOf(4, 5, 6, 7)
 * println("Common elements: ${findCommonElements(list1, list2)}")  // Output: [4, 5]
 *
 */
fun findCommonElements(list1: List<Int>, list2: List<Int>): List<Int> {
    TODO("Not yet implemented")
}